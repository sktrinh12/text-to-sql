[project]
# Name shown in the browser tab and header
name = "Text-to-SQL"

[UI]
# Text shown next to the logo in the header
name = "Text-to-SQL"

# Tagline shown on the welcome screen (under the logo)
description = "Ask questions about the database in plain English."

# Collapse the chain-of-thought steps by default (keeps UI clean)
default_collapse_content = true

# custom_css = "/public/custom.css"

# "light" or "dark"
default_theme = "dark"

# login page image
# login_page_image = "public/background-image.jpeg"

[UI.theme]
# Primary brand colour (used for buttons, highlights)
primary = "#4de7aa"

[meta]
generated_by = "1.0.0"
